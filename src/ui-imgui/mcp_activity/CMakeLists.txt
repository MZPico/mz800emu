# src/ui-imgui/mcp_activity/CMakeLists.txt - MCP Activity okno (placeholder)
#
# V této fázi (V0.A.1) NENÍ tento soubor zapojen do top-level buildu.
# Projekt používá centralizovaný GLOB scan ze `cmake/AddMzEmu.cmake`
# (`mz_glob_sources src/ui-imgui` rekurzivně). Soubor existuje jako
# placeholder + dokumentační kotva pro budoucí refaktor.
#
# Pokud V0.A.4+ tento podstrom dostane vlastní static library, postup
# bude analogický jako u `src/emulator/mcp/CMakeLists.txt`.
